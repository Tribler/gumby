experiment_name = TriblerTests

local_setup_cmd = tribler_experiment_setup.sh

nose_run_dir = tribler

nose_tests_to_run = Tribler/Test/

isolated_tribler_instances_to_spawn = 5

process_guard_timeout = 300

#isolated_cmd = 'process_guard.py -t $PROCESS_GUARD_TIMEOUT -i 5 -c "wrap_in_vnc.sh gumby/experiments/tribler_idle_run/tribler_idle_run.py" -m $OUTPUT_DIR -o $OUTPUT_DIR'
#isolated_cmd = 'sleep $PROCESS_GUARD_TIMEOUT'

local_instance_cmd = 'process_guard.py -c "stats_crawler.sh" -t $PROCESS_GUARD_TIMEOUT -m $OUTPUT_DIR/crawler  -o $OUTPUT_DIR/crawler'

#home_file = '/home/corpaul/workspace/gumby/experiments/tribler_idle_run/tribler_home_cp.tar.gz'

post_process_cmd = graph_process_guard_data.sh

use_local_venv = False

tracker_port = __unique_port__

pythonpath = '/home/jenkins/workspace/Experiment_Bartercast_local/tribler/'
#pythonpath = '/home/corpaul/workspace/tribler/'
