
workspace_dir = "./"
local_setup_cmd = "gorilla_setup.sh"
remote_setup_cmd = "das4_setup.sh"
# Commands/scripts to use to spawn the instances in the local/remote nodes.
# Those paths are relative to the experiment dir
local_instance_cmd = "wrap_in_vnc.sh python gumby/experiments/tribler_idle_run/run_for_30m.py"
tracker_cmd = ""
# Enable use of virtualenv when running on the local machine (will be always used on the DAS4)
use_local_venv = True
# Enable use of systemtap (Will be never used on DAS4)
use_local_systemtap = True
