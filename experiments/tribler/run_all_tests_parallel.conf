experiment_name = TriblerTestsParallel

local_setup_cmd = tribler_experiment_setup.sh

nose_run_dir = tribler

nose_tests_parallelisation = 10

nose_tests_to_run = src/tribler-core/tribler_core/

local_instance_cmd = run_nosetests_for_jenkins.sh

post_process_cmd = graph_process_guard_data.sh

use_local_venv = False

# Python version to use during tests (either py2 or py3)
python_version = py2
