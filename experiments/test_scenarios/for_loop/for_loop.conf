experiment_name = "for_loop_experiment"
experiment_time = 300

sync_port = __unique_port__
sync_host = 127.0.0.1
sync_subscribers_amount = 10

local_instance_cmd = "process_guard.py -c launch_scenario.py -n 10 -t $EXPERIMENT_TIME -m $OUTPUT_DIR  -o $OUTPUT_DIR "

scenario_file = 'for_loop.scenario'

use_local_venv = FALSE

